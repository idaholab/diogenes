# tools / schemas

Various schemas supporting the Livewire Data Platform and its tools.

## Sharing

The following folders are shared in different locations on the larger project's repository as follows:

- `./catalog/` -> `schemas/catalog/` (all files)
- `./dictionary/` -> `schemas/dictionary/` (all files)
- `./other/` -> `tools/schemas/other/` (all files)
